meta {
  name: Get All Users
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/users
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  baseUrl: http://localhost:8080
}

docs {
  # Get All Users
  
  Obtiene la lista de todos los usuarios desde JSONPlaceholder API.
  
  ## Autenticación
  Requiere token JWT en el header Authorization.
  
  Ejecuta primero el request "Login" para obtener el token.
}
